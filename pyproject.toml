[project]
name = "game-tracker-toy"
version = "0.1.0"
description = "Toy system design implementation for score and friend leaderboards"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
